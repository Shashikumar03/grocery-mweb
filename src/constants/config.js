export const APP_NAME = "Grocery Portal";

/** Used when `VITE_API_BASE_URL` is unset (direct calls). Postman: {{grocery_base_url}}/api/... */
export const DEFAULT_API_BASE_URL = "https://grocery-app-env.up.railway.app";
