import { Screen } from "../../components/common/Screen.jsx";

export function CartPage() {
  return (
    <Screen title="Cart">
      <p className="muted">Line items, promos, proceed to checkout.</p>
    </Screen>
  );
}
