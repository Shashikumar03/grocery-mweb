[build]
  command = "npm run build"
  publish = "dist"

# Same as public/_redirects — ensures client-side routes work even if
# Netlify build settings or monorepo layout differ.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

