# API + auth proxy (same-origin as in Vite dev). Must be BEFORE the SPA fallback.
# Origin must match src/constants/config.js DEFAULT_API_BASE_URL when using empty VITE_API_BASE_URL.
/api/*   https://grocery-app-env.up.railway.app/api/:splat   200
/auth/*  https://grocery-app-env.up.railway.app/auth/:splat  200

# SPA fallback for React Router
/*       /index.html   200
